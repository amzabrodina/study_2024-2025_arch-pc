 клавиату;------------------------------------------------------------------
;------------------- Объявление переменных ------------ .bss buf1: RESB 80
          ; символ перевода строки
          ; Длина переменной 'msg'
;екция не инициированных данных
; Буфер размером 80 байт
Архитектура ЭВМ
   
Архитектура ЭВМ
 ;-------------------
SION .text GLOBAL _start
_start:
Текст программы    -----------------
mov 
mov  ebx,1
mov  ecx,msg
mov  edx,msgLen
